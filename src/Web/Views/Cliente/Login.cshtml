@using SuporteSolidario.ViewModel
@model LoginViewModel

@{
    ViewData["Title"] = "Login do Cliente";
    string? loginInvalido = (string?) ViewData["LoginInvalido"];
    ViewData["LoginInvalido"] = "";
}

<h1>@ViewData["Title"]</h1>
<h4>Aqui teremos uma página de login</h4>

<section>
    <div class="page-header min-vh-75">
        <div class="container">
            <!--
            <div class="row">
                <div class="col-xl-6 col-lg-8 col-md-12 col-12 d-flex flex-column mx-auto">
                    <div class="card card-plain mt-8">
                        <div class="card-header pb-0 text-left bg-transparent">
                            <p class="mb-0">Informe o seu login e senha</p>
                        </div>
                        <div class="card-body">
                            <form asp-action="Login" method="post"  >
                                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                                <label asp-for="Login"></label>
                                <div class="mb-3">
                                    <input asp-for="Login" class="form-control border"/>
                                    <span asp-validation-for="Login" class="text-danger" ></span>
                                </div>
                                <div class="mb-3">
                                    <label asp-for="Password"></label>
                                    <input asp-for="Password" class="form-control border"/>
                                    <span asp-validation-for="Password" class="text-danger" ></span>
                                </div>
                                <div class="mb-3">
                                    <label asp-for="RememberMe">
                                        <input asp-for="RememberMe" />
                                        @Html.DisplayNameFor(x => x.RememberMe)
                                    </label>
                                </div>
                                <div class="text-center">
                                    <input type="submit" value="Acessar" class="btn bg-gradient-info w-100 mt-4 mb-0"></input>
                                </div>
                            </form>
                        </div>
                        
                    </div>
                </div>
            </div>
            @if(!string.IsNullOrEmpty(loginInvalido))
            {
            <div class="row">
                <div class="col-12 text-center">
                    <label class="control-label" style="color: #FF5555;">@loginInvalido</label>
                </div>
            </div>
            }
            -->

            <div class="row">
                <img src="~/img/rascunhos/login.jpeg" width="100px" height="550px" />
            </div>
        </div>
    </div>
</section>